version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: folha-bet365-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db_folha_bet365
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
