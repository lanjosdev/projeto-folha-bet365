generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Machine {
  id        String   @id @db.Char(36)
  createdAt DateTime @default(now()) @map("created_at")

  @@map("machines")
}
